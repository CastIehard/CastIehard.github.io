{
  "name": "castlehard-md",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "dev": "npm run build && python3 -m http.server 3000 --directory dist"
  }
}
